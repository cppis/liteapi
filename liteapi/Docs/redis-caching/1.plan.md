# Redis 캐싱 도입 계획

EF Core DB 성능 개선을 위한 Redis 인메모리 캐싱 추가 계획

## 핵심 원칙

> **캐시 장애가 서비스 장애가 되어서는 안 된다.**
>
> Redis는 성능 최적화 수단이며, 핵심 의존성이 아니다.
> Redis 연결 실패, 타임아웃, 직렬화 오류 등 어떤 캐시 장애가 발생하더라도
> LiteApi는 DB 직접 조회로 정상 동작해야 한다.

이 원칙은 모든 구현 단계에 적용되며, 캐시 관련 코드는 반드시 다음을 준수한다:
- 모든 캐시 작업을 try-catch로 감싸고, 예외 시 로그만 남긴 뒤 DB로 폴백
- Redis를 optional dependency로 취급 (Redis 없이도 서비스 기동 가능)
- 헬스체크에서 Redis 상태는 `Degraded`로 보고 (서비스 전체를 `Unhealthy`로 만들지 않음)

## 1. 현재 상태 분석

### 데이터 접근 패턴
현재 모든 User CRUD 엔드포인트가 `AppDbContext`를 직접 사용하여 MySQL에 매번 쿼리를 실행한다.

| 엔드포인트 | 쿼리 패턴 | 캐싱 가치 |
|-----------|----------|----------|
| `GET /api/users` | `ToListAsync()` | 높음 - 전체 조회 빈도 높음 |
| `GET /api/users/{id}` | `FindAsync(id)` | 높음 - PK 조회, 캐시 적중률 높음 |
| `PUT /api/users/{id}` | `FindAsync` + `SaveChangesAsync` | 캐시 무효화 대상 |
| `DELETE /api/users/{id}` | `FindAsync` + `Remove` | 캐시 무효화 대상 |
| `POST /api/users/{id}/add-gold` | `FindAsync` + `SaveChangesAsync` | 캐시 무효화 대상 |
| `POST /api/users` | `Add` + `SaveChangesAsync` | 캐시 무효화 대상 |

### 문제점
- 읽기 요청마다 MySQL 네트워크 왕복 발생
- 동일 사용자를 반복 조회해도 매번 DB 쿼리 실행
- 전체 사용자 목록 조회 시 부하 증가 가능성

## 2. 목표

- 읽기 성능 향상: 자주 조회되는 데이터를 Redis에 캐싱
- DB 부하 감소: 불필요한 반복 쿼리 제거
- 데이터 정합성 유지: 쓰기 작업 시 캐시 무효화
- 기존 코드 최소 변경: 서비스 레이어 추가로 엔드포인트 로직 변경 최소화

## 3. 기술 선택

### 패키지
```xml
<PackageReference Include="StackExchange.Redis" Version="2.8.x" />
<PackageReference Include="Microsoft.Extensions.Caching.StackExchangeRedis" Version="9.0.x" />
```

### 선택 이유
- `StackExchange.Redis`: .NET Redis 클라이언트 표준, 높은 성능과 안정성
- `IDistributedCache` 인터페이스: ASP.NET Core 내장 캐시 추상화 활용 가능

## 4. 구현 계획

### 4.1단계: Redis 연결 설정

**appsettings.yaml에 Redis 설정 추가:**
```yaml
Redis:
  Connection: "localhost:6379"
  InstanceName: "liteapi:"
  DefaultTTLSeconds: 300    # 기본 캐시 만료 5분
```

**Program.cs에 서비스 등록:**
```csharp
// Redis 연결
builder.Services.AddStackExchangeRedisCache(options =>
{
    options.Configuration = builder.Configuration["Redis:Connection"];
    options.InstanceName = builder.Configuration["Redis:InstanceName"];
});
```

→ 검증: Redis 연결 확인, 헬스체크에 Redis 상태 추가

### 4.2단계: CacheService 구현

**새 파일: `Services/CacheService.cs`**

```csharp
public class CacheService
{
    private readonly IDistributedCache _cache;
    private readonly IConfiguration _configuration;
    private readonly ILogger<CacheService> _logger;

    // 캐시 키 규칙
    // - 단일 사용자: "user:{userId}"
    // - 전체 사용자 목록: "users:all"

    public async Task<T?> GetAsync<T>(string key);
    public async Task SetAsync<T>(string key, T value, TimeSpan? ttl = null);
    public async Task RemoveAsync(string key);
    public async Task RemoveByPrefixAsync(string prefix);
}
```

DI 등록: `builder.Services.AddSingleton<CacheService>();`

→ 검증: 단위 테스트로 Get/Set/Remove 동작 확인

### 4.3단계: 읽기 엔드포인트에 캐시 적용

**GET /api/users/{id} — Cache-Aside 패턴:**
```
1. Redis에서 "user:{userId}" 조회
2. 캐시 히트 → 즉시 반환
3. 캐시 미스 → DB 조회 → Redis에 저장 → 반환
```

**GET /api/users — 목록 캐싱:**
```
1. Redis에서 "users:all" 조회
2. 캐시 히트 → 즉시 반환
3. 캐시 미스 → DB 조회 → Redis에 저장 (짧은 TTL) → 반환
```

→ 검증: 동일 요청 2회 실행 시 두 번째 요청에서 DB 쿼리 미발생 확인

### 4.4단계: 쓰기 엔드포인트에 캐시 무효화 적용

| 쓰기 작업 | 무효화 대상 |
|----------|-----------|
| `POST /api/users` (생성) | `users:all` |
| `PUT /api/users/{id}` (수정) | `user:{id}`, `users:all` |
| `DELETE /api/users/{id}` (삭제) | `user:{id}`, `users:all` |
| `POST /api/users/{id}/add-gold` (골드 추가) | `user:{id}`, `users:all` |

무효화 전략: **Write-Through** — DB 저장 성공 후 캐시 삭제 (다음 읽기 시 재캐싱)

→ 검증: 사용자 수정 후 조회 시 최신 데이터 반환 확인

### 4.5단계: Prometheus 메트릭 추가

**MetricsService에 캐시 메트릭 추가:**
```csharp
// Counter
private readonly Counter _cacheHitsTotal;    // 캐시 히트 수
private readonly Counter _cacheMissesTotal;  // 캐시 미스 수

// Histogram
private readonly Histogram _cacheOperationDuration;  // 캐시 작업 소요 시간
```

→ 검증: `/metrics` 엔드포인트에서 캐시 관련 메트릭 확인

### 4.6단계: 테스트 작성

**단위 테스트:**
- `CacheServiceTests.cs` — Get/Set/Remove 동작, TTL 만료, 직렬화/역직렬화
- 기존 엔드포인트 테스트에 캐시 히트/미스 시나리오 추가

**통합 테스트:**
- `test-cache.http` — 캐시 동작 확인용 HTTP 테스트 파일

→ 검증: `dotnet test` 전체 통과

## 5. 캐시 키 설계

```
liteapi:user:{userId}     → 단일 사용자 JSON
liteapi:users:all          → 전체 사용자 목록 JSON
```

- prefix `liteapi:`는 Redis InstanceName으로 자동 적용
- TTL: 단일 사용자 300초, 전체 목록 60초 (목록은 변경 빈도가 높으므로 짧게)

## 6. 직렬화 방식

기존 프로젝트가 JSON과 MessagePack을 모두 지원하므로, 캐시 저장 시에도 선택 가능:
- **JSON**: 디버깅 용이, Redis CLI에서 직접 확인 가능
- **MessagePack**: 크기 50-70% 감소, 더 빠른 직렬화/역직렬화

초기에는 **JSON**으로 시작하고, 성능 요구 시 MessagePack으로 전환.

## 7. 에러 처리 전략 (핵심 원칙 적용)

**캐시 장애 = 서비스 장애가 되어서는 안 된다.**

### CacheService 내부 처리
모든 public 메서드에서 캐시 관련 예외를 흡수한다:

```csharp
public async Task<T?> GetAsync<T>(string key)
{
    try
    {
        var cached = await _cache.GetStringAsync(key);
        if (cached is null) return default;
        return JsonSerializer.Deserialize<T>(cached);
    }
    catch (Exception ex)
    {
        _logger.LogWarning(ex, "캐시 읽기 실패: {Key}. DB로 폴백", key);
        return default;  // 캐시 미스로 처리 → 호출자가 DB 조회
    }
}

public async Task SetAsync<T>(string key, T value, TimeSpan? ttl = null)
{
    try
    {
        var json = JsonSerializer.Serialize(value);
        await _cache.SetStringAsync(key, json, new DistributedCacheEntryOptions
        {
            AbsoluteExpirationRelativeToNow = ttl ?? _defaultTTL
        });
    }
    catch (Exception ex)
    {
        _logger.LogWarning(ex, "캐시 쓰기 실패: {Key}. 무시하고 계속 진행", key);
        // 저장 실패해도 서비스에 영향 없음
    }
}

public async Task RemoveAsync(string key)
{
    try
    {
        await _cache.RemoveAsync(key);
    }
    catch (Exception ex)
    {
        _logger.LogWarning(ex, "캐시 삭제 실패: {Key}. TTL 만료에 의존", key);
        // 삭제 실패 시 TTL에 의해 자연 만료됨
    }
}
```

### 장애 시나리오별 동작

| 장애 상황 | 동작 | 서비스 영향 |
|----------|------|-----------|
| Redis 연결 실패 | 캐시 무시, DB 직접 조회 | **없음** |
| Redis 타임아웃 | 로그 경고, DB 폴백 | **없음** |
| 캐시 직렬화 오류 | 해당 키 무시, DB 조회 | **없음** |
| Redis 서버 다운 | 모든 캐시 미스, DB 전량 처리 | **없음** (성능 저하만 발생) |
| Redis 데이터 유실 | 자연스러운 캐시 미스 | **없음** |

### 서비스 기동 시
Redis 연결 실패 시에도 서비스가 정상 기동되어야 한다:
```csharp
// Program.cs — Redis를 optional로 등록
try
{
    builder.Services.AddStackExchangeRedisCache(options => { ... });
}
catch (Exception ex)
{
    logger.LogWarning(ex, "Redis 연결 실패. 캐시 없이 서비스 기동");
    builder.Services.AddDistributedMemoryCache();  // 폴백: 인메모리 캐시
}
```

### 헬스체크
Redis 장애 시 전체 헬스체크를 `Unhealthy`로 만들지 않는다:
```csharp
builder.Services.AddHealthChecks()
    .AddRedis(redisConnection, name: "redis",
        failureStatus: HealthStatus.Degraded);  // Unhealthy가 아닌 Degraded
```

## 8. 파일 변경 목록

| 파일 | 변경 내용 |
|-----|---------|
| `liteapi.csproj` | Redis 패키지 추가 |
| `appsettings.yaml` | Redis 연결 설정 추가 |
| `appsettings.Development.yaml` | 개발 환경 Redis 설정 |
| `Program.cs` | Redis 서비스 등록, 엔드포인트 캐시 로직 |
| `Services/CacheService.cs` | **신규** — 캐시 서비스 |
| `Services/MetricsService.cs` | 캐시 메트릭 추가 |
| `Tests/Services/CacheServiceTests.cs` | **신규** — 캐시 테스트 |
| `test-cache.http` | **신규** — HTTP 테스트 파일 |

## 9. 실행 순서 요약

```
1. [패키지 추가]       → 검증: dotnet restore 성공
2. [Redis 연결 설정]   → 검증: 헬스체크에서 Redis 상태 확인
3. [CacheService 구현] → 검증: 단위 테스트 통과
4. [읽기 캐싱 적용]    → 검증: 반복 조회 시 DB 쿼리 감소 확인
5. [캐시 무효화 적용]  → 검증: 쓰기 후 조회 시 최신 데이터 반환
6. [메트릭 추가]       → 검증: /metrics에서 캐시 히트/미스 확인
7. [테스트 작성]       → 검증: dotnet test 전체 통과
```

## 10. 향후 확장 고려사항

- **Redis Pub/Sub**: 다중 인스턴스 환경에서 캐시 무효화 전파 (장애 시 TTL 만료로 폴백)
- **Rate Limiting**: Redis 기반 API 호출 제한 (장애 시 제한 없이 통과로 폴백)
